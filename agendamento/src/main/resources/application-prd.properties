spring.application.name=agendamento

# Configuração do MongoDB para Azure Cosmos DB
spring.data.mongodb.uri=mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@bd-fiap-api-dev.documents.azure.com:10255/<nome-do-database>?ssl=true&replicaSet=globaldb

# Configuração do Flyway para migrações
spring.flyway.baselineOnMigrate=true

# Outras configurações do Spring
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.hibernate.ddl-auto=update

# Configuração de erros
server.error.include-message=always
server.error.include-stacktrace=always
server.error.include-binding-errors=always





